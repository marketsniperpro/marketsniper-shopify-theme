{% comment %}
  For pages with JSON templates, content_for_layout should contain the sections
  For regular pages, show basic content
{% endcomment %}

{{ content_for_layout }}

{% if content_for_layout == blank %}
  <div class="container page-content">
    <h1>{{ page.title }}</h1>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
{% endif %}