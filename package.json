{
  "name": "marketsniper-website",
  "version": "1.0.0",
  "description": "Market Sniper - Trading Bot Suite Website",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && netlify deploy --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@stripe/stripe-js": "^2.4.0"
  }
}
